{
  "name": "netlify-test",
  "version": "1.0.0",
  "description": "stuff",
  "main": "index.js",
  "scripts": {
    "build1": "rm -rf dist && rm -rf ../dist-temp && mkdir dist && mkdir ../dist-temp && cp -r ./* ../dist-temp && cp -r ../dist-temp/* dist && rm -rf ../dist-temp",
    "build2": "rm -rf dist && mkdir dist && rsync -rv * dist --exclude ./dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/electrovir/netlify-test.git"
  },
  "author": "electrovir",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/electrovir/netlify-test/issues"
  },
  "homepage": "https://github.com/electrovir/netlify-test#readme",
  "dependencies": {
    "lodash": "^4.17.11"
  }
}
